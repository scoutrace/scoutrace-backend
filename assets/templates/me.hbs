<h1>Mig mig mig mig mig!</h1>

{{!#form class="form"}}
<form>
	<div class="form-group">{{!label 'name' 'Navn'}}<label>Navn</label> {{input class="form-control"}}</div>
	<div class="form-group"><label>E-mail</label> {{input class="form-control"}}</div>
	<div class="form-group"><label>Ny kode</label> {{input class="form-control" type="password"}}</div>
	<div class="form-group"><label>Ny kode (gentag)</label> {{input class="form-control" type="password"}}</div>

	{{!submit "save" "Gem"}}
	<input type="submit" class="btn btn-primary" value="Gem" />
{{!/form}}
</form>